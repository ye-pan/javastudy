package com.yp.designpatterns.composition;

import com.yp.designpatterns.Coord;

public class ArrayCompositor implements Compositior {
    @Override
    public int compose(Coord[] natural, Coord[] stretch, Coord[] shrink, int compoentCount, int lineWidth, int[] breaks) {
        return 0;
    }
}
